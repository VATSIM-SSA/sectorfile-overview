name: Deploy Plugins vCH

on:
  push:
    paths:
      - "Plugins/vCH/**"
    branches:
      - main

permissions: {}

jobs:
  deploy_plugin_vch_afrc:
    name: "Deploy vCH to AFRC"
    runs-on: ubuntu-latest
    steps:
      - name: Push files to another repository
        uses: nkoppel/push-files-to-another-repository@v1.1.3
        env:
          API_TOKEN_GITHUB: ${{ secrets.ACT_COPY_TO_REPO_PAT }}
        with:
          source-files: "Plugins/vCH/VCH.dll"
          destination-username: "VATSIM-SSA"
          destination-repository: "sectorfile-afrc"
          destination-branch: "main"
          destination-directory: "Plugins/vCH"
          commit-email: "30835922+miguelalbano@users.noreply.github.com"
          commit-message: "update: vch dll"

  deploy_plugin_vch_afrs:
    name: "Deploy vCH to AFRS"
    runs-on: ubuntu-latest
    steps:
      - name: Push files to another repository
        uses: nkoppel/push-files-to-another-repository@v1.1.3
        env:
          API_TOKEN_GITHUB: ${{ secrets.ACT_COPY_TO_REPO_PAT }}
        with:
          source-files: "Plugins/vCH/VCH.dll"
          destination-username: "VATSIM-SSA"
          destination-repository: "sectorfile-afrs"
          destination-branch: "main"
          destination-directory: "Plugins/vCH"
          commit-email: "30835922+miguelalbano@users.noreply.github.com"
          commit-message: "update: vch dll"

  deploy_plugin_vch_afrw:
    name: "Deploy vCH to AFRW"
    runs-on: ubuntu-latest
    steps:
      - name: Push files to another repository
        uses: nkoppel/push-files-to-another-repository@v1.1.3
        env:
          API_TOKEN_GITHUB: ${{ secrets.ACT_COPY_TO_REPO_PAT }}
        with:
          source-files: "Plugins/vCH/VCH.dll"
          destination-username: "VATSIM-SSA"
          destination-repository: "sectorfile-afrw"
          destination-branch: "main"
          destination-directory: "Plugins/vCH"
          commit-email: "30835922+miguelalbano@users.noreply.github.com"
          commit-message: "update: vch dll"

  deploy_plugin_vch_dgac:
    name: "Deploy vCH to DGAC"
    runs-on: ubuntu-latest
    steps:
      - name: Push files to another repository
        uses: nkoppel/push-files-to-another-repository@v1.1.3
        env:
          API_TOKEN_GITHUB: ${{ secrets.ACT_COPY_TO_REPO_PAT }}
        with:
          source-files: "Plugins/vCH/VCH.dll"
          destination-username: "VATSIM-SSA"
          destination-repository: "sectorfile-dgac"
          destination-branch: "main"
          destination-directory: "Plugins/vCH"
          commit-email: "30835922+miguelalbano@users.noreply.github.com"
          commit-message: "update: vch dll"

  deploy_plugin_vch_dnkk:
    name: "Deploy vCH to DNKK"
    runs-on: ubuntu-latest
    steps:
      - name: Push files to another repository
        uses: nkoppel/push-files-to-another-repository@v1.1.3
        env:
          API_TOKEN_GITHUB: ${{ secrets.ACT_COPY_TO_REPO_PAT }}
        with:
          source-files: "Plugins/vCH/VCH.dll"
          destination-username: "VATSIM-SSA"
          destination-repository: "sectorfile-dnkk"
          destination-branch: "main"
          destination-directory: "Plugins/vCH"
          commit-email: "30835922+miguelalbano@users.noreply.github.com"
          commit-message: "update: vch dll"

  deploy_plugin_vch_fajo:
    name: "Deploy vCH to FAJO"
    runs-on: ubuntu-latest
    steps:
      - name: Push files to another repository
        uses: nkoppel/push-files-to-another-repository@v1.1.3
        env:
          API_TOKEN_GITHUB: ${{ secrets.ACT_COPY_TO_REPO_PAT }}
        with:
          source-files: "Plugins/vCH/VCH.dll"
          destination-username: "VATSIM-SSA"
          destination-repository: "sectorfile-fajo"
          destination-branch: "main"
          destination-directory: "Plugins/vCH"
          commit-email: "30835922+miguelalbano@users.noreply.github.com"
          commit-message: "update: vch dll"

  deploy_plugin_vch_fasa:
    name: "Deploy vCH to FASA"
    runs-on: ubuntu-latest
    steps:
      - name: Push files to another repository
        uses: nkoppel/push-files-to-another-repository@v1.1.3
        env:
          API_TOKEN_GITHUB: ${{ secrets.ACT_COPY_TO_REPO_PAT }}
        with:
          source-files: "Plugins/vCH/VCH.dll"
          destination-username: "VATSIM-SSA"
          destination-repository: "sectorfile-fasa"
          destination-branch: "main"
          destination-directory: "Plugins/vCH"
          commit-email: "30835922+miguelalbano@users.noreply.github.com"
          commit-message: "update: vch dll"

  deploy_plugin_vch_fbgr:
    name: "Deploy vCH to FBGR"
    runs-on: ubuntu-latest
    steps:
      - name: Push files to another repository
        uses: nkoppel/push-files-to-another-repository@v1.1.3
        env:
          API_TOKEN_GITHUB: ${{ secrets.ACT_COPY_TO_REPO_PAT }}
        with:
          source-files: "Plugins/vCH/VCH.dll"
          destination-username: "VATSIM-SSA"
          destination-repository: "sectorfile-fbgr"
          destination-branch: "main"
          destination-directory: "Plugins/vCH"
          commit-email: "30835922+miguelalbano@users.noreply.github.com"
          commit-message: "update: vch dll"

  deploy_plugin_vch_fccc:
    name: "Deploy vCH to FCCC"
    runs-on: ubuntu-latest
    steps:
      - name: Push files to another repository
        uses: nkoppel/push-files-to-another-repository@v1.1.3
        env:
          API_TOKEN_GITHUB: ${{ secrets.ACT_COPY_TO_REPO_PAT }}
        with:
          source-files: "Plugins/vCH/VCH.dll"
          destination-username: "VATSIM-SSA"
          destination-repository: "sectorfile-fccc"
          destination-branch: "main"
          destination-directory: "Plugins/vCH"
          commit-email: "30835922+miguelalbano@users.noreply.github.com"
          commit-message: "update: vch dll"

  deploy_plugin_vch_fimm:
    name: "Deploy vCH to FIMM"
    runs-on: ubuntu-latest
    steps:
      - name: Push files to another repository
        uses: nkoppel/push-files-to-another-repository@v1.1.3
        env:
          API_TOKEN_GITHUB: ${{ secrets.ACT_COPY_TO_REPO_PAT }}
        with:
          source-files: "Plugins/vCH/VCH.dll"
          destination-username: "VATSIM-SSA"
          destination-repository: "sectorfile-fimm"
          destination-branch: "main"
          destination-directory: "Plugins/vCH"
          commit-email: "30835922+miguelalbano@users.noreply.github.com"
          commit-message: "update: vch dll"

  deploy_plugin_vch_flfi:
    name: "Deploy vCH to FLFI"
    runs-on: ubuntu-latest
    steps:
      - name: Push files to another repository
        uses: nkoppel/push-files-to-another-repository@v1.1.3
        env:
          API_TOKEN_GITHUB: ${{ secrets.ACT_COPY_TO_REPO_PAT }}
        with:
          source-files: "Plugins/vCH/VCH.dll"
          destination-username: "VATSIM-SSA"
          destination-repository: "sectorfile-flfi"
          destination-branch: "main"
          destination-directory: "Plugins/vCH"
          commit-email: "30835922+miguelalbano@users.noreply.github.com"
          commit-message: "update: vch dll"

  deploy_plugin_vch_fmmm:
    name: "Deploy vCH to FMMM"
    runs-on: ubuntu-latest
    steps:
      - name: Push files to another repository
        uses: nkoppel/push-files-to-another-repository@v1.1.3
        env:
          API_TOKEN_GITHUB: ${{ secrets.ACT_COPY_TO_REPO_PAT }}
        with:
          source-files: "Plugins/vCH/VCH.dll"
          destination-username: "VATSIM-SSA"
          destination-repository: "sectorfile-fmmm"
          destination-branch: "main"
          destination-directory: "Plugins/vCH"
          commit-email: "30835922+miguelalbano@users.noreply.github.com"
          commit-message: "update: vch dll"

  deploy_plugin_vch_fnan:
    name: "Deploy vCH to FNAN"
    runs-on: ubuntu-latest
    steps:
      - name: Push files to another repository
        uses: nkoppel/push-files-to-another-repository@v1.1.3
        env:
          API_TOKEN_GITHUB: ${{ secrets.ACT_COPY_TO_REPO_PAT }}
        with:
          source-files: "Plugins/vCH/VCH.dll"
          destination-username: "VATSIM-SSA"
          destination-repository: "sectorfile-fnan"
          destination-branch: "main"
          destination-directory: "Plugins/vCH"
          commit-email: "30835922+miguelalbano@users.noreply.github.com"
          commit-message: "update: vch dll"

  deploy_plugin_vch_fqbe:
    name: "Deploy vCH to FQBE"
    runs-on: ubuntu-latest
    steps:
      - name: Push files to another repository
        uses: nkoppel/push-files-to-another-repository@v1.1.3
        env:
          API_TOKEN_GITHUB: ${{ secrets.ACT_COPY_TO_REPO_PAT }}
        with:
          source-files: "Plugins/vCH/VCH.dll"
          destination-username: "VATSIM-SSA"
          destination-repository: "sectorfile-fqbe"
          destination-branch: "main"
          destination-directory: "Plugins/vCH"
          commit-email: "30835922+miguelalbano@users.noreply.github.com"
          commit-message: "update: vch dll"

  deploy_plugin_vch_fsss:
    name: "Deploy vCH to FSSS"
    runs-on: ubuntu-latest
    steps:
      - name: Push files to another repository
        uses: nkoppel/push-files-to-another-repository@v1.1.3
        env:
          API_TOKEN_GITHUB: ${{ secrets.ACT_COPY_TO_REPO_PAT }}
        with:
          source-files: "Plugins/vCH/VCH.dll"
          destination-username: "VATSIM-SSA"
          destination-repository: "sectorfile-fsss"
          destination-branch: "main"
          destination-directory: "Plugins/vCH"
          commit-email: "30835922+miguelalbano@users.noreply.github.com"
          commit-message: "update: vch dll"

  deploy_plugin_vch_fvhf:
    name: "Deploy vCH to FVHF"
    runs-on: ubuntu-latest
    steps:
      - name: Push files to another repository
        uses: nkoppel/push-files-to-another-repository@v1.1.3
        env:
          API_TOKEN_GITHUB: ${{ secrets.ACT_COPY_TO_REPO_PAT }}
        with:
          source-files: "Plugins/vCH/VCH.dll"
          destination-username: "VATSIM-SSA"
          destination-repository: "sectorfile-fvhf"
          destination-branch: "main"
          destination-directory: "Plugins/vCH"
          commit-email: "30835922+miguelalbano@users.noreply.github.com"
          commit-message: "update: vch dll"

  deploy_plugin_vch_fwll:
    name: "Deploy vCH to FWLL"
    runs-on: ubuntu-latest
    steps:
      - name: Push files to another repository
        uses: nkoppel/push-files-to-another-repository@v1.1.3
        env:
          API_TOKEN_GITHUB: ${{ secrets.ACT_COPY_TO_REPO_PAT }}
        with:
          source-files: "Plugins/vCH/VCH.dll"
          destination-username: "VATSIM-SSA"
          destination-repository: "sectorfile-fwll"
          destination-branch: "main"
          destination-directory: "Plugins/vCH"
          commit-email: "30835922+miguelalbano@users.noreply.github.com"
          commit-message: "update: vch dll"

  deploy_plugin_vch_fywf:
    name: "Deploy vCH to FYWF"
    runs-on: ubuntu-latest
    steps:
      - name: Push files to another repository
        uses: nkoppel/push-files-to-another-repository@v1.1.3
        env:
          API_TOKEN_GITHUB: ${{ secrets.ACT_COPY_TO_REPO_PAT }}
        with:
          source-files: "Plugins/vCH/VCH.dll"
          destination-username: "VATSIM-SSA"
          destination-repository: "sectorfile-fywf"
          destination-branch: "main"
          destination-directory: "Plugins/vCH"
          commit-email: "30835922+miguelalbano@users.noreply.github.com"
          commit-message: "update: vch dll"

  deploy_plugin_vch_fzza:
    name: "Deploy vCH to FZZA"
    runs-on: ubuntu-latest
    steps:
      - name: Push files to another repository
        uses: nkoppel/push-files-to-another-repository@v1.1.3
        env:
          API_TOKEN_GITHUB: ${{ secrets.ACT_COPY_TO_REPO_PAT }}
        with:
          source-files: "Plugins/vCH/VCH.dll"
          destination-username: "VATSIM-SSA"
          destination-repository: "sectorfile-fzza"
          destination-branch: "main"
          destination-directory: "Plugins/vCH"
          commit-email: "30835922+miguelalbano@users.noreply.github.com"
          commit-message: "update: vch dll"

  deploy_plugin_vch_gooo:
    name: "Deploy vCH to GOOO"
    runs-on: ubuntu-latest
    steps:
      - name: Push files to another repository
        uses: nkoppel/push-files-to-another-repository@v1.1.3
        env:
          API_TOKEN_GITHUB: ${{ secrets.ACT_COPY_TO_REPO_PAT }}
        with:
          source-files: "Plugins/vCH/VCH.dll"
          destination-username: "VATSIM-SSA"
          destination-repository: "sectorfile-gooo"
          destination-branch: "main"
          destination-directory: "Plugins/vCH"
          commit-email: "30835922+miguelalbano@users.noreply.github.com"
          commit-message: "update: vch dll"

  deploy_plugin_vch_goooo:
    name: "Deploy vCH to GOOOO"
    runs-on: ubuntu-latest
    steps:
      - name: Push files to another repository
        uses: nkoppel/push-files-to-another-repository@v1.1.3
        env:
          API_TOKEN_GITHUB: ${{ secrets.ACT_COPY_TO_REPO_PAT }}
        with:
          source-files: "Plugins/vCH/VCH.dll"
          destination-username: "VATSIM-SSA"
          destination-repository: "sectorfile-goooo"
          destination-branch: "main"
          destination-directory: "Plugins/vCH"
          commit-email: "30835922+miguelalbano@users.noreply.github.com"
          commit-message: "update: vch dll"

  deploy_plugin_vch_gvsc:
    name: "Deploy vCH to GVSC"
    runs-on: ubuntu-latest
    steps:
      - name: Push files to another repository
        uses: nkoppel/push-files-to-another-repository@v1.1.3
        env:
          API_TOKEN_GITHUB: ${{ secrets.ACT_COPY_TO_REPO_PAT }}
        with:
          source-files: "Plugins/vCH/VCH.dll"
          destination-username: "VATSIM-SSA"
          destination-repository: "sectorfile-gvsc"
          destination-branch: "main"
          destination-directory: "Plugins/vCH"
          commit-email: "30835922+miguelalbano@users.noreply.github.com"
          commit-message: "update: vch dll"

  deploy_plugin_vch_hkna:
    name: "Deploy vCH to HKNA"
    runs-on: ubuntu-latest
    steps:
      - name: Push files to another repository
        uses: nkoppel/push-files-to-another-repository@v1.1.3
        env:
          API_TOKEN_GITHUB: ${{ secrets.ACT_COPY_TO_REPO_PAT }}
        with:
          source-files: "Plugins/vCH/VCH.dll"
          destination-username: "VATSIM-SSA"
          destination-repository: "sectorfile-hkna"
          destination-branch: "main"
          destination-directory: "Plugins/vCH"
          commit-email: "30835922+miguelalbano@users.noreply.github.com"
          commit-message: "update: vch dll"

  deploy_plugin_vch_htdc:
    name: "Deploy vCH to HTDC"
    runs-on: ubuntu-latest
    steps:
      - name: Push files to another repository
        uses: nkoppel/push-files-to-another-repository@v1.1.3
        env:
          API_TOKEN_GITHUB: ${{ secrets.ACT_COPY_TO_REPO_PAT }}
        with:
          source-files: "Plugins/vCH/VCH.dll"
          destination-username: "VATSIM-SSA"
          destination-repository: "sectorfile-htdc"
          destination-branch: "main"
          destination-directory: "Plugins/vCH"
          commit-email: "30835922+miguelalbano@users.noreply.github.com"
          commit-message: "update: vch dll"

  deploy_plugin_vch_huec:
    name: "Deploy vCH to HUEC"
    runs-on: ubuntu-latest
    steps:
      - name: Push files to another repository
        uses: nkoppel/push-files-to-another-repository@v1.1.3
        env:
          API_TOKEN_GITHUB: ${{ secrets.ACT_COPY_TO_REPO_PAT }}
        with:
          source-files: "Plugins/vCH/VCH.dll"
          destination-username: "VATSIM-SSA"
          destination-repository: "sectorfile-huec"
          destination-branch: "main"
          destination-directory: "Plugins/vCH"
          commit-email: "30835922+miguelalbano@users.noreply.github.com"
          commit-message: "update: vch dll"
